function logout() {
  // Perform any logout logic here, e.g., clearing session, redirecting to login page, etc.
  window.location.href = "login.html"; // Redirect to login.html after logout
}
function index() {
  // Perform any logout logic here, e.g., clearing session, redirecting to login page, etc.
  window.location.href = "index.html"; // Redirect to login.html after logout
}
